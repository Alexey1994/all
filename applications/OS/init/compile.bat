cls && @nasm "16 bit\loader.asm" -o bin\loader && @echo 16 bit code compiled && @gcc -w -s -Iinclude -fno-builtin -nostdinc -nostdlib -c "32 bit/main.c" && echo 32 bit code compiled && @ld -T script.ld -o kernel.bin main.o && @objcopy -O binary -R .note -R .comment -S kernel.bin bin/code && @echo linked && @"save to virtual disk.exe" && @echo saved to virtual disk && @clean.bat && @echo success